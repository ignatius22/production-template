services:
  api:
    build:
      context: ./apps/api

    container_name: atlas-api

    ports:
      - "3000:3000"

    restart: unless-stopped

    env_file:
      - .env

networks:
  default:
    name: atlas-network